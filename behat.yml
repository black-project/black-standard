default:

    autoload:
        "":  %paths.base%/features/bootstrap

    suites:
        default:
            contexts:
                - FeatureContext
            filters: { tags: "@ui" }

domain:

    autoload:
        "":  %paths.base%/features/bootstrap

    suites:
        default:
            contexts:
                - DomainContext
            filters: { tags: "@domain" }

    extensions:
        RMiller\BehatSpec\BehatExtension:
            path: bin/phpspec
